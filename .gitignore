bin/
obj/
artifacts/
TestResults/
*.user
*.suo
.vs/
.idea/
*.swp
.superpowers/
appsettings.Local.json

# Local nupkg feed staged by docker/build-local.ps1 for the Docker build context.
packages/*
!packages/.gitkeep

# Docker local secrets (the .env.example template stays tracked).
docker/.env
docker/certs/

# Advanced Installer build output and the scratch files verify-aip.ps1 -Build / the designer leave
# next to the project (the .aip itself stays tracked).
setups/AdvancedInstaller/Karmasis.MiniVault/Setup Files/
setups/AdvancedInstaller/Karmasis.MiniVault/*-cache/
setups/AdvancedInstaller/Karmasis.MiniVault/*.check.aip
setups/AdvancedInstaller/Karmasis.MiniVault/*.back(*).aip
